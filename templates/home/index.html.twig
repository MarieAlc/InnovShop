{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<h1>Bienvenue sur InnovShop !</h1>

<div class="homepage-container">
  {% for sectionTitle, articles in {
    '📰 Articles à la une': aLaUne,
    '🆕 Nouveautés': derniersArticles
  } %}
    <section class="section">
      <h2>{{ sectionTitle }}</h2>
      <div class="articles-grid">
        {% for article in articles %}
          <div class="article-card">
            <img src="{{ asset('uploads/articles/' ~ article.image) }}" alt="{{ article.titre }}">
            <div class="article-info">
              <h3>{{ article.titre }}</h3>
              <p>{{ article.detail|slice(0, 100) ~ '...' }}</p>
              <a href="{{ path('article_detail', { id: article.id }) }}" class="btn">Voir plus</a>
            </div>
          </div>
        {% endfor %}
      </div>
    </section>
  {% endfor %}
</div>
{% endblock %}
